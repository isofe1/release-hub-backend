# GitHub Personal Access Token (fine-grained or classic).
# Only needs public repo read access ("public_repo" scope, or read-only on public repositories).
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
